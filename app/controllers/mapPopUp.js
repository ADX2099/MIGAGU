//$.mapWebView.scalesPageToFit = false;
// $.popupMap.showMap = function(texto, link) {
	// Ti.API.info("El url es:.." + link);
	// //$.mapWebView.url = Url;
	// $.mapWebView.url = link;
// 
	// var t = Titanium.UI.create2DMatrix();
	// t = t.scale(1);
// 
	// $.popupMap.transform = t;
// 
	// var t1 = Titanium.UI.create2DMatrix();
	// t1 = t1.scale(0);
// 
	// var a = Titanium.UI.createAnimation();
	// a.transform = t1;
	// a.duration = 200;
// 
	// a.addEventListener('complete', function() {
		// //Titanium.API.info('here in complete');
		// var t2 = Titanium.UI.create2DMatrix();
		// t2 = t2.scale(1.0);
		// $.popupMap.animate({
			// transform : t2,
			// duration : 200
		// });
	// });
// 
	// $.aceptarticket.addEventListener("click", function() {
		// $.popupMap.close();
	// });
	// $.consulta.value = texto;
// 
// $.aceptar.addEventListener("click", function() {
	// $.popupWin.visible = false;
// });
	// $.getDescripcion.addEventListener("click", function() {
		// Ti.API.info("El texto que regreso:.." + texto);
		// $.popupWin.visible = true;
	// });
// 
// }

